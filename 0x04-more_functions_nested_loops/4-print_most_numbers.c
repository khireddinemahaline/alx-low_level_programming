#include "main.h"
/**
 * print_most_numbers - print all single number not 2 and 4
 */
void print_most_numbers(void)
{
	int n;

	for (n = 48; n <= 57; n++)
	{
		if (!(c == 50 || c == 52))
			_putchar(n);
	}
	_putchar('/n');
}
