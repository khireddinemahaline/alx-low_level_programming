#include "main.h"
/**
 * print_alphabet - function print alpha in lower case
 *
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
	int i;

	for (i = 48; i <= 57; i++)
		_putchar(i);
	_putchar('\n');
	return (0);
}
