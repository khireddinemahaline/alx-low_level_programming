#include <stdio.h>
#include <stdlib.h>
/**
 * main - this is the entry point of the code
 * @argc : number of argument
 * @argv : array of argument
 * Return 0 Successful
 */

int main(int argc, char *argv[])
{
	printf("the name of progrme is %s\n", argv[0]);
	return (0);
}
